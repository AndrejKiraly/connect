
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.name=__Host-SESSIONID

# TODO: consider __Host- prefix for the CSRF cookie too (blocks cookie tossing from a subdomain)
#  breaks SPA auto-detection of the XSRF-TOKEN name -> decide with frontend

# TODO: switch FRAMEWORK -> NATIVE + trusted-proxies when proxy IPs known
server.forward-headers-strategy=FRAMEWORK